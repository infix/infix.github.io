import Typography from "typography"

export default new Typography({
  baseFontSize: "18px",
  baseLineHeight: 1.666,
  headerFontFamily: ["Ubuntu", "san-serif"],
  bodyFontFamily: ["Ubuntu", "san-serif"],
})

